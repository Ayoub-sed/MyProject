import { classes } from "./classes";
import { departement } from "./departement";

export class student {
    id: number;
    classeId:number;
    studentName: string;
    birthDate:Date;
    classe:classes;
}